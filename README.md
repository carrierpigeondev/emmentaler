# emmentaler
emmental(rend)er (engine)

## in early development

### impl

starting with reading toml files in distributed directory, combine into one for all `[[items]]`

### todo:

basic shape for simple 2d render items, logging and not rendering

raylib window setup

simple render runtime with raylib
